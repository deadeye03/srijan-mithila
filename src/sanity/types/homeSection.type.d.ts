interface NavSchemaType {
    _id: string
    title: string,
    icon: string,
    navMenu: string[]

}

interface SliderShowSchemaType {
    title: string,
    images: string[]
}

interface HeroSchemaType {
    title: string,
    slug: string,
    imageUrl: string
}